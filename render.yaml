services:
  - type: web
    name: neuridion-web
    env: node
    buildCommand: npm install && npm run verify:env -- --mode production && npm run build
    startCommand: npm start
    envVarGroups:
      - neuridion-env
